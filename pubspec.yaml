name: yt_creator_boost
description: "A Flutter app for YouTube creators to discover and grow their channels"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter

  # Test with just Firebase Core first
  firebase_core: ^2.32.0

  # UI Utils
  flutter_screenutil: ^5.9.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.2

flutter:
  uses-material-design: true